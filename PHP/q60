60. Write a PHP function to search a specified value within the values of an associative array.
ANSWER
<?php
function searchValueInAssocArray($array, $targetValue) {
    // Check if it's a valid array
    if (!is_array($array)) {
        return null;
    }
   // Search for the value
    $result = array_keys($array, $targetValue, true); // true for strict comparison

    // Return result (array of keys or empty array if not found)
    return $result;
}
// Example usage
$assocArray = [
    "name" => "Alice",
    "age" => 30,
    "city" => "New York",
    "profession" => "Engineer",
    "nickname" => "Alice"
];
$search = "Alice";
$foundKeys = searchValueInAssocArray($assocArray, $search);
// Display the result
if (!empty($foundKeys)) {
    echo "Value '$search' found in keys: " . implode(", ", $foundKeys);
} else {
    echo "Value '$search' not found in the array.";
}
?>

OUTPUT
Value 'Alice' found in keys: name, nickname
