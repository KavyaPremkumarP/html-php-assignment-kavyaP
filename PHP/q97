97. Discover the various session functions of PHP. Explain its behavior.

Common PHP Session Functions and Explanation & Behavior

1.session_start()	
Starts a new session or resumes an existing one
session_start();
 Starts a session
 Must be called before any output
 session_start()

2.session_id()	
Gets or sets the current session ID
 echo session_id(); // Get session ID
 session_id("abc123"); // Set a custom session ID (before session_start)

3.session_name()	
Gets or sets the name of the session (usually PHPSESSID)
 echo session_name(); // Output: PHPSESSID
 session_name("MYSESSION"); // Set before session_start()
 
4.session_save_path()
Gets or sets the path where session data is stored
 session_save_path()
  Gets or sets the directory where session files are stored.
  echo session_save_path();
  session_save_path("/tmp/sessions"); // Set before session_start()

5.session_status()	
Returns the current session status
 session_status()
  Returns session state:
  $status = session_status();
  if ($status === PHP_SESSION_ACTIVE) {
    echo "Session is active.";
  }

6.session_destroy()
Destroys all data registered to a session
 session_start();
 session_destroy();

7.session_unset()
Frees all session variables
 Clears all variables in $_SESSION, but does not destroy the session itself.
 session_unset();

8.session_regenerate_id()	
Replaces the current session ID with a new one
 session_regenerate_id()
 Creates a new session ID to prevent session fixation attacks.
 session_start();
 session_regenerate_id();

9.session_encode()	
Encodes the current session data as a string

10.session_decode()	
Decodes a session data string and loads it into the session

11.session_get_cookie_params()	
Gets the parameters of the session cookie
session_set_cookie_params(3600, "/", "example.com", true, true);

12.session_set_cookie_params()
Sets the session cookie parameters
session_set_cookie_params(3600, "/", "example.com", true, true);

13.session_cache_limiter()	
Gets or sets the cache limiter

14.session_cache_expire()	
Gets or sets the session cache expiration time
